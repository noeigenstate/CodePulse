/** UI-local refresh cadence for derived realtime labels. */
export const UI_REFRESH_INTERVAL_MS = 500

/** Renderer status fallback polling; IPC push remains the primary path. */
export const STATUS_REFRESH_INTERVAL_MS = 1_000
