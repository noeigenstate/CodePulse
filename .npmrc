# Hoist Electron tooling so electron-builder / electron-vite can find binaries
public-hoist-pattern[]=*electron*
public-hoist-pattern[]=*vite*
# better-sqlite3 needs to be rebuilt against the Electron ABI
node-linker=hoisted
