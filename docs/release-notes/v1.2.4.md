## v1.2.4

### 更新内容

#### 应用内更新提醒

- 后台仍按约 **6 小时** 检查一次 GitHub 最新版；有新版本时弹出更新提示。
- 用户选择「稍后」不更新时，**24 小时内**不再检测、不再弹窗（写入本机 `update-snooze.json`，重启后仍生效）。
- 用户选择更新时清除静默，并直接安装 **最新 Release**（无需逐版升级）。
- 补充更新检测间隔与 snooze 策略的单元测试。
