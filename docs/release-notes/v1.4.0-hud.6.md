## v1.4.0-hud.6

### HUD 项目卡片临时关闭

- 每张项目卡片右上角新增小型 `×`，可以只暂时关闭当前 agent 卡片，同项目的其他 agent 不受影响。
- 关闭不会终止任务或改写实际生命周期；纯用量刷新不会重新显示卡片，后续真实活动仍按原有触发逻辑自动恢复。
- “完成/已读”保持接近旧版尺寸的完整圆角矩形，并始终占用固定控制位；按钮是否出现都不会挤压或移动前方信息。
- `×` 位于卡片坐标的最右上角和自身内边距范围内，不占用“完成/已读”按钮区域。
