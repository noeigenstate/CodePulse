## v1.1.1

### 更新内容

#### 安装包统计后台

- 修复安装包中本地历史库打不开：补回 `better-sqlite3` 加载原生模块所需的 `bindings` 依赖，并改为显式解析 `.node` 路径。
- 统计后台可区分「库未就绪 / 当前时间段无历史 / 聚合失败」，并展示本机 SQLite 路径，便于排查。
- 减轻大库统计扫描负担，避免后台长时间卡在同步中。

#### 其它

- 完善 README：本地统计后台说明、中英文各自截图（实时控制台 + 后台）。
- 修复更新检查相关单元测试的 TypeScript 类型问题。
